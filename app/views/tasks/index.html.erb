<h1>Tasks List</h1>

<li><%= link_to( "Home", index_path ) %></li>
<li><%= link_to( "New Task", new_path ) %></li>

<p>
  <% @tasks.each do |task| %>
  <ul>
    <li><%= link_to( task.title, show_path(task.id) ) %></li>
  </ul>
  <% end %>
</p>
